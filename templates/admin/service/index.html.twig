{% extends 'headerFooter.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="/styles/card.css">
    <link rel="stylesheet" href="/styles/headerFooter.css">
    <link rel="stylesheet" href="/styles/globalFont.css">
    <script src="https://unpkg.com/@phosphor-icons/web"></script>
    <script src="https://unpkg.com/@phosphor-icons/web@2.1.1"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script crossorigin src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script crossorigin src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Jersey+10&display=swap" rel="stylesheet">
{% endblock %}

{% block body %}

    <div class="button-group">
        <a href="{{ path('app_service_new') }}" class="button">
            <i class="ph ph-plus-circle"></i>
        </a>
    </div>

    <div class="cards-container">
        {% for service in services %}
            <div class="card">
                {% if service.image is not null %}
                    <img src="{{ '/uploads/services/' ~ service.image.path}}" alt="{{ service.nom }}">
                {% endif %}
                <div class="card-info">
                    <h3>{{ service.nom }}</h3>
                </div>
                <div class="card-actions">
                    <a href="{{ path('app_service_edit', { id: service.id }) }}"><i class="ph ph-note-pencil"></i></a>
                    <form method="post" action="{{ path('app_service_delete', { id: service.id }) }}" style="display:inline">
                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
                        <button type="submit" class="delete-btn" onclick="return confirm('Êtes-vous sûr ?')"><i class="ph ph-trash"></i></button>
                    </form>
                </div>
            </div>
        {% endfor %}
    </div>
{% endblock %}
